{
  "name": "greencheck-e2e",
  "private": true,
  "description": "Browser-level end-to-end tests: real Chromium against all three live services (frontend + web-service + analysis-service), fully offline and deterministic.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.0"
  }
}
